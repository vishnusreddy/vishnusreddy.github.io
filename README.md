# Vishnu Sadashiva Reddy

A plain HTML and CSS portfolio. No JavaScript, framework, or dependencies.

Run locally:

```sh
python3 -m http.server 3000 --bind 127.0.0.1
```

Open http://localhost:3000. GitHub Pages can serve the files directly from the repository root.
